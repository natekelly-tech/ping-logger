# ping-logger

A simple command-line tool that checks if a list of hosts are reachable and logs the results to a file.

## Usage

    python ping_logger.py

## Requirements

- Python 3.x

## Output